name: cjh_buili
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi
      - uvicorn[standard]
      - sqlalchemy
      - pydantic-settings
      - python-multipart
      - aiofiles
      - httpx
      - reportlab
      - pymupdf
      - pillow
      - opencv-python-headless
      - numpy
      - pandas
      - scikit-learn
      - joblib
      - datasets
      - huggingface_hub
      - git+https://github.com/huggingface/transformers
      - accelerate
      - peft
      - bitsandbytes
      - qwen-vl-utils
      - sentence-transformers
      - safetensors
      - tqdm
      - pytest
      - ruff
