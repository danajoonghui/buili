name: cjh_buili
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi
      - uvicorn[standard]
      - sqlalchemy
      - psycopg[binary]
      - redis
      - boto3
      - pydantic-settings
      - python-multipart
      - aiofiles
      - httpx
      - reportlab
      - pymupdf
      - pillow
      - opencv-python-headless
      - shapely
      - trimesh
      - easyocr
      - numpy
      - pandas
      - scikit-learn
      - joblib
      - datasets
      - huggingface_hub
      - git+https://github.com/huggingface/transformers
      - accelerate
      - peft
      - bitsandbytes
      - qwen-vl-utils
      - sentence-transformers
      - safetensors
      - requests
      - remotezip
      - ultralytics
      - tqdm
      - pytest
      - ruff
